trigger:
- none

pool:
  vmImage: 'equisoft-testing'
  agent.name: equisoft-testing

steps:
- task: Bash@3
  displayName: "List Directory Contents"
  inputs:
    targetType: 'inline'
    script: |
      # List the contents of the current directory
      echo "Listing contents of the current directory:"
      ls -la
